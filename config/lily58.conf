# --- [기존에 작성하신 설정들] ---
# Uncomment the following line to enable deep sleep
# CONFIG_ZMK_SLEEP=y

# Uncomment the following line to increase the keyboard's wireless range
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Enable eager debouncing
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=1
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=10

# Enable ZMK Studio for Realtime Keymap Updates
CONFIG_ZMK_STUDIO=n
CONFIG_ZMK_STUDIO_LOCKING=n

# Uncomment the following line to enable USB Logging
# CONFIG_ZMK_USB_LOGGING=y

CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y

# --- [공식 문서 기준: zmk-nice-oled 연동 설정 추가] ---
# 1. zmk-nice-oled 모듈 그래픽 엔진 활성화
CONFIG_NICE_OLED_ON=y

# 2. 왼쪽 화면에 위젯 시스템 및 타자 속도(WPM) 그래프 켜기
CONFIG_NICE_OLED_WIDGET_STATUS=y
CONFIG_NICE_OLED_WIDGET_WPM=y

# 3. 왼쪽 화면에 봉고캣 애니메이션 최종 활성화
CONFIG_NICE_OLED_WIDGET_WPM_BONGO_CAT=y
