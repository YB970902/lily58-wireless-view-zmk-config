# --- [왼쪽 화면: 봉고캣 설정] ---
# 1. 디스플레이 기본 기능 활성화
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y

# 2. nice!view (Memory LCD) 전력 및 드라이버 설정
CONFIG_NICE_EPAPER_ON=y
CONFIG_ZMK_DISPLAY_BLANK_ON_IDLE=y

# 3. zmk-nice-oled 모듈 활성화
CONFIG_NICE_OLED_ON=y

# 4. 왼쪽(마스터) 화면에 봉고캣 활성화 설정 (핵심)
CONFIG_NICE_OLED_WIDGET_STATUS=y
CONFIG_NICE_OLED_WIDGET_WPM=y
CONFIG_NICE_OLED_WIDGET_WPM_BONGO_CAT=y

# 봉고캣 애니메이션 속도 설정 (밀리초 단위, 낮을수록 역동적임)
CONFIG_NICE_OLED_WIDGET_WPM_BONGO_CAT_ANIMATION_MS=300

# --- [오른쪽 화면: 애니메이션 설정] ---
# 1. 우측 화면 애니메이션 마스터 스위치 켜기
CONFIG_NICE_OLED_WIDGET_ANIMATION_PERIPHERAL=y

# 2. 원하는 캐릭터 하나만 y로 켜기 (여기서는 포켓몬)
CONFIG_NICE_OLED_WIDGET_ANIMATION_PERIPHERAL_POKEMON=y
